#! /bin/bash

rate4site -s HRH1_aligned_cleaned.fasta -t HRH1.tre -m Mw -im -bh

### This will produce the output of interest `r4sOrig.res`, which was manually converted to the csv `r4s.csv` for visualization.